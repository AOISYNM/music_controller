# music_controller
🎵 Music Controller — A full-stack web app built with Django REST Framework + React (TypeScript) that lets users create and join shared music rooms. Hosts can configure playback permissions and skip votes, while guests can control music in real time based on room settings
